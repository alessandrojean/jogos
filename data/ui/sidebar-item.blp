using Gtk 4.0;

template $SidebarItem : ListBoxRow {
  Box {
    orientation: horizontal;
    spacing: 12;
    styles ["sidebar-item"]

    Image itemIcon {
      styles ["sidebar-icon"]
    }

    Label itemLabel {}
  }
}
